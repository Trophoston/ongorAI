numpy>=1.24
opencv-python>=4.8
tensorflow>=2.15
pyserial>=3.5
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- pipeline ใหม่ MediaPipe ---
# mediapipe>=0.10   # Optional: uncomment if your Python version has pre-built wheels
scikit-learn>=1.3   # สำหรับ train_mediapipe.py (LabelEncoder, train_test_split)
pandas>=2.0         # อ่าน CSV ใน train_mediapipe.py

# --- เฉพาะตอน inference บนบอร์ด (ทางเลือก, เบากว่า tensorflow มาก) ---
# pip install tflite-runtime        # หรือ ai-edge-litert
# --- pipeline เก่า PoseNet (ถ้ายังต้องการ) ---
# pip install tfjs-graph-converter
