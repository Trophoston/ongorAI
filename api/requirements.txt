# Pose-predict API — รันด้วย BlazePose ผ่าน TFLite ตรง ๆ (ไม่ใช้แพ็กเกจ mediapipe)
# เหตุผล: mediapipe ไม่มี wheel สำหรับ Linux aarch64 (เช่น Arduino Uno Q) เลย
# เราจึงรันโมเดล pose_landmark_full.tflite (อยู่ใน models/blazepose/) เองผ่าน
# tflite interpreter ซึ่งมี wheel aarch64 ให้
#
# ตัวรันโมเดล .tflite เลือกตามเวอร์ชัน Python ของบอร์ด (โค้ดลองโหลดตามลำดับนี้):
#   * Python 3.11 หรือต่ำกว่า  ->  tflite-runtime   (เบาสุด, แนะนำ)
#   * Python 3.12 / 3.13       ->  ai-edge-litert   (มี wheel aarch64 ให้ 3.12/3.13)
#   * เครื่อง dev (เช่น Mac)     ->  tensorflow       (ตัวสำรองสุดท้าย, หนัก)
numpy
opencv-python
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# เลือกอย่างใดอย่างหนึ่งตามเวอร์ชัน Python (ดู INSTALL_ARDUINO_UNO_Q.md):
tflite-runtime ; python_version < "3.12"
ai-edge-litert ; python_version >= "3.12"
